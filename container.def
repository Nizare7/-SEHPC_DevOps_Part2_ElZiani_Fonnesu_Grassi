Bootstrap: docker
From: ubuntu:20.04

%post
    apt update && apt install -y cmake && apt install -y git  && apt install -y cmake && apt install -y mpich
    cd /usr/src/DevOps
    mkdir -p build && cd build
    cmake ..
    make
       
%files
    . /usr/src/DevOps

%runscript
    cd /usr/src/DevOps/build
    ./test_multiplication
